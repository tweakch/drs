# Fail fast if the active Node version does not satisfy package.json#engines.
engine-strict=true

# Deterministic installs in CI / Vercel.
prefer-frozen-lockfile=true

# pnpm hoists peer deps automatically (default, pinned explicitly for clarity).
auto-install-peers=true
